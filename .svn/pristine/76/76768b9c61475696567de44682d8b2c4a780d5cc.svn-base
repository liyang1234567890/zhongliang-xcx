<!--pages/mine/category/cateSub/cateSub.wxml-->
<view>
  <!-- <view class='cateSub' wx:for='{{sub}}' wx:key='{{index}}'> -->
    <!-- <view class='sub-title'>{{item.subName}}</view> -->
    <view class='sub-cont'>
      <view class="sub-sub {{selected==item.category_id?'selected':''}}" wx:for='{{sub}}' wx:key='{{index}}' bindtap='click' data-id='{{item.category_id}}'>{{item.category_name}}</view>
    </view>
  <!-- </view> -->
  <button type='primary' class='add' bindtap='add'>加入品类</button>
</view>